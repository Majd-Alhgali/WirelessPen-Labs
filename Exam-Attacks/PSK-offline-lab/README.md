# PSK Offline Lab

Purpose: attempt controlled offline recovery against the authorized `Taim Starlink`
handshake without router access, phishing, WPS, or open-ended brute force.

Primary capture:

```text
../../TaimStarlink-Analysis_Files/taim_client_3e_test-01.cap
```

Primary hash:

```text
taim_3e.22000
```

Local files:

- `base.txt`: small environment-derived candidate seed list.
- `logs/`: command logs and status outputs.
- `results/`: cracked output and reports if successful.

Do not commit cracked passwords or potfiles.
